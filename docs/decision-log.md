# Decision log — design history and rationale

This document records WHY the tool is shaped the way it is — what alternatives we considered and why they didn't fit, and the chronological log of decisions made during development. For setup, see the [README](../README.md). For implementation reference, see [internals.md](internals.md).

---

## 1. Goal & constraints

The design was shaped by a specific set of constraints that ruled out simpler approaches:

**Hard requirements:**
- No print dialog.
- No per-print user interaction (one-time setup is fine).
- Printer remains usable by other applications on the same machine.

**Existing OpenMRS module behavior:** server-side ZPL generation, then
pushed to network printers over a raw TCP socket on port 9100. The
client-side path needs a different transport but should reuse the same
ZPL where possible. This meant the transport layer had to be swappable
without touching the ZPL generation logic already in the module.

**Client OSes:** primarily Windows; should also work on Ubuntu and macOS.

These constraints together ruled out `window.print()` (dialog), WebUSB
(removes the printer from the OS on Windows — see §2), and any approach
that required driver modification or exclusive device ownership.

---

## 2. Options surveyed and rejected

### Print.js (already in this folder)
**Rejected.** Print.js generates an iframe and calls `window.print()`. That
still triggers Chrome's print preview, violating the "no dialog" requirement.
The file is left in the directory only because it was already there.

### `window.print()` / OS print system via Zebra driver
**Rejected.** Same dialog problem. Even with kiosk-print flags, you're at the
mercy of Chrome's print policy and the driver's behavior, and you give up
fine control over ZPL.

### WebUSB (talk to the printer's bulk endpoint directly from the page)
**Considered, then rejected.** Zero-install on the *page* side, but each OS
has a friction point that blocks the path to production:

- **Windows:** the OS's `usbprint.sys` claims the device. To make it
  WebUSB-visible you have to swap the driver to WinUSB (via Zadig). That
  removes the printer from the OS print system, so other apps can no longer
  use it — directly violating one of our constraints.
- **Linux:** the kernel `usblp` module auto-claims the printer-class
  interface; you'd need a udev rule + module blacklist or per-attach unbind.
- **macOS:** generally works, but if CUPS has the printer added it'll fight
  WebUSB for the device.
- **Pairing:** Chrome requires a one-time `requestDevice()` user gesture per
  origin/profile. That's small but not zero.

WebUSB is the cleanest "browser native" demo, but it doesn't survive contact
with the production constraint that the printer must remain usable by the OS.
We did not pursue it.

### Zebra Browser Print
**Selected.** Small native helper app (Windows MSI / macOS PKG / Linux DEB)
that runs in the user session and exposes a localhost HTTP service. The page
talks to it via Zebra's official JavaScript SDK. The helper sends bytes
through the OS's installed Zebra driver, so the printer remains a normal
system printer.

Trade-off: each client needs the helper installed once. In return: no driver
swaps, no kernel-module fights, no pairing prompt, USB *and* network printers,
official Zebra support, and parsed printer status.

### PDF Direct on the printer
**Not available on the GX430t.** PDF Direct is a Link-OS firmware option on
ZT400/ZT600 series and some ZD620 models — those printers can take a `.pdf`
straight on the wire. The GX430t's firmware has no PDF interpreter, so any
"send a PDF" workflow has to convert to ZPL on the client before transport.

---

## 3. Chronological decision log

Each entry captures a discrete moment where the design or implementation
shifted — things tried, things broken, things learned.

| Step | What happened |
|---|---|
| 1 | Started from the existing module's network-socket ZPL printing, looking for client-side equivalents in the browser. |
| 2 | Surveyed: Print.js (rejected — dialog), `window.print()` (rejected — dialog), WebUSB (rejected — Windows driver swap removes the printer from the OS), Zebra Browser Print (selected). |
| 3 | First prototype hit Browser Print's HTTP API directly via `fetch()`. Worked, but didn't handle the HTTP↔HTTPS port switch and didn't surface parsed printer status. |
| 4 | Discovered the SDK and the helper application are *separate* downloads — adding the SDK alone isn't sufficient. |
| 5 | Discovered (by reading `BrowserPrint-3.1.250.min.js`) that the helper actually listens on **two** ports: 9100 (HTTP) and 9101 (HTTPS, self-signed). The SDK picks based on the page's protocol. |
| 6 | Discovered (in the SDK's bundled JSDoc) that PDF conversion via the helper is gated on a Zebra `featureKey` license. Image conversions are not. |
| 7 | Refactored to use the SDK throughout (`BrowserPrint.getDefaultDevice`, `device.send`, `Zebra.Printer.getStatus`), added a third pathway via `Zebra.Printer.getConvertedResource`, wired post-print status reads. |
| 8 | Discovered Zebra no longer distributes a Linux Browser Print build on the main downloads page (only Windows / macOS / Android). |
| 9 | Built `browser-print-shim.py` — a stdlib-only Python service that mirrors the Browser Print HTTP API contract (extracted from `BrowserPrint-3.1.250.min.js`). Supports both transports the GX430t actually uses: USB (via the kernel `usblp` device node, which is bidirectional, so status reads work) and network (raw TCP on port 9100, persistent socket so `~HS` query/response works). Validated end-to-end with `curl`. |
| 10 | Added the user's Zebra PDF feature key as a default in the prototype HTML so pathway 3 doesn't need re-pasting on each load. (Security note: don't commit the file with the key intact to a public repo.) |
| 11 | Tried pathway 3 against the shim, hit the 501 from `/convert` — implemented `/convert` in the shim using Ghostscript + Floyd-Steinberg + ZPL `^GF` run-length compression. The shim now advertises `api_level: 4` and `supportedConversions: {"pdf": ["zpl"]}` when `gs` is on the PATH, so all three pathways can be exercised on Linux dev boxes. Output isn't bit-identical to Zebra's licensed converter but is a credible substitute. |
| 12 | Realised the original "pathway 2" (PDF rasterized in the browser via pdf.js + naive luminance threshold + raw `^GFA`) had become strictly worse than the shim's helper conversion on every axis — quality, payload size, dependencies — and that its only unique property ("works without any helper for the *render* step") was moot because a helper is required for the print transport regardless. Removed pathway 2 entirely; pdf.js dependency dropped; old "pathway 3" promoted to "pathway 2". Page is ~130 lines smaller. |
| 13 | Aligned the direct-ZPL layout (margins, font sizes, barcode size and position) with what the PDF pathway produces, so labels from both pathways come out the same physical size for direct apples-to-apples comparison. Fixed a `^GF` byte-alignment bug in the shim's compression (`rstrip('0')` could strip a single nibble from the last byte when width % 8 ≠ 0). Added stale-USB-buffer drain on device open in the shim (was the likely cause of "Invalid Response" after a shim restart). |
| 14 | This README. |
| 15 | Confirmed the `DEFAULT_FEATURE_KEY` in `browser-print.html` is the *public* PDF demo key Zebra hands out for prototyping (hardcoded plaintext on Zebra's own test harness at `cagdemo.com/BrowserPrint/test/external/zebra_test.html`, with Zebra's developer forum thread 25874 directing users there to copy it). Kept the key in the repo with a citation comment instead of stripping it. Documented PDF Direct (`device.sendFile(pdfBlob)`) as an alternative pathway that needs no `featureKey` if the printer firmware supports it — best production answer if the deployed GX430t fleet has it enabled. |
| 16 | Promoted this prototype out of `openmrs-module-printer/testing/` into a standalone repo at `pih/zebra-printing-tools` — it had grown beyond a "testing" scratchpad (shim, README, SDK, multiple pathways) and the dependency direction is the other way around (the OpenMRS module would consume tools from here, not the other way). |
| 17 | Tracked spurious red "Offline" pills + indefinite "querying…" on the status row to the SDK's STX/ETX framing check on `~HS` responses (`offline = true` whenever the trimmed body doesn't *both* start with `0x02` and end with `0x03`). The actual `~HS` reply is three STX/ETX-framed records flushed back-to-back; the shim's old `/read` returned after the first `select()` fired, often capturing only the first frame. Fixed at the shim layer with `_drain_until_quiet_fd` / `_drain_until_quiet_sock` (drain until 150 ms of silence, 1500 ms hard cap). Belt-and-braces on the page: race each `getStatus` against a 500 ms timeout, retry up to 3× silently on offline / timeout / error, surface the truthful end state. ~2.3 s worst case for both auto-fire and manual click. |
| 18 | Generalised the prototype off the GX430t. Resolution dropdown → numeric input + datalist; new width / height inputs in inches. On device selection the page now fires `head.resolution.in_dpi` SGD + `^XA^HH^XZ` + `~hi` in parallel through the SDK queue, then resolves dpi by escalating fallbacks (canonical SGD → `cfg.settings.RESOLUTION` → model-string regex → `device.host_resolution` SGD). The GX430t we tested doesn't expose the canonical SGD or a RESOLUTION field; detection succeeds on the model-string fallback (`"GX430t-300dpi"` → 300). User edits are sticky — re-detection won't overwrite them; reselect the printer to wipe overrides and detect afresh. Detection has its own 5 s per-query timeout (separate from the status budget) because `getSGD` uses `sendThenReadAllAvailable`, which recurses until `/read` returns empty — costing ~2.25 s per SGD call even on a fast printer. |
| 19 | Promoted the page from "demo with hidden detection" to "general Zebra setup / troubleshooting tool". Added a Printer info section (model / firmware / Link-OS / dpi / connection / UID) populated from the same detection cycle that fills the geometry inputs; merged the previously-separate Status section into it (status row spans the full info-grid width with a thin underline) and replaced the two refresh buttons with a single ↻ Refresh that re-runs status + detection together. New Diagnostics section with eight preset commands (`~hi`, `~HS`, `^HH`, `^HZa`, `getvar "*"`, `^WC`, `~JC`, `~JR`) plus a free-form custom-command textarea; presets are tagged `read` or `write` so query commands wait for a response and fire-and-forget commands skip the read. Raw-response viewer shows control bytes as `<STX>`/`<ETX>`/etc. with a 16-byte/row hex dump beneath. Diagnostics deliberately bypass the SDK's queued Request machinery (uses `device.send` / `device.read` directly) for predictable single-shot behaviour and to avoid contending with the SDK's auto-fired `configTimeout` cycle. Light visual rework: subtle 1 px rules between adjacent non-pathway sections, generous vertical margin, info card uses a grid with monospace values. |
| 20 | Caught that the existing dpi parser dropped `"1280 12/MM FULL"` because it grabbed the leading integer (head width in dots, not dpi) and the "1280" failed the range gate. Replaced `parseDpiCandidate` with `parseDensity` returning both `{ dpi, dpm }` — when an `N/mm` token is present the page now also captures the *exact* dots-per-mm density. Added a unit toggle (in / mm) for the Label-geometry section: changing units flips the resolution-field label between `dpi` and `dots/mm`, swaps its datalist (`152/203/300/600` ↔ `6/8/12/24`), updates the suffix on the width/height inputs, and reflows the Printer-info card's Resolution row to match (with the other representation in parens when known). Page defaults to whatever the printer reports — auto-switches to mm on detection when `densityDpm` is captured, *not* persisted (no localStorage). User toggling sets a session-only `userOverrodeUnit` flag so re-selection of the same printer doesn't bounce them back. The toggle converts width/height via *dots*, not via the naïve `× 25.4` factor — so the toggle preserves physical print output exactly even when the labelled dpi (300) and underlying density (12/mm = 304.8 dpi) round differently. Density itself converts via the standard `6 ↔ 152`, `8 ↔ 203`, `12 ↔ 300`, `24 ↔ 600` mapping; non-standard densities go through `× 25.4` (lossy by ≤1.6% in the unusual case). |
| 21 | Tried twice to bring the form-based PDF (pathway 2) and direct ZPL (pathway 1) outputs into closer visual alignment by tuning ZPL font heights / barcode module width — both attempts ended up worse, because (a) ZPL ^A0's visible-cap-to-cell ratio is ~75 % not the 85 % I'd assumed, so reducing 58 → 48 over-shrunk; (b) ZPL Code 128 auto-optimises mixed-letter+digit data to subset C (90 modules instead of 112), so my dynamic `^BY` calculation under-filled even after correction; (c) the GX430t firmware treats `>9` subset directives as literal data rather than encoding directives, so forcing subset B is firmware-dependent. Lesson: the two pathways have inherently different rendering pipelines (printer firmware native vs vector-rasterised) and there's no portable native-ZPL-only way to match them exactly. Reverted to the original ZPL output and *flipped the calibration direction* per user direction: changed `buildPdf` to match the ZPL output instead. New PDF rendering uses 1:1 canvas-px-to-printer-dot scale (`width: 6` in JsBarcode, drawn at `bc.width × 72/300` pt), matching ZPL's `^BY 6` directly; PDF font sizes bumped to 17 pt header / 13 pt body so Helvetica's visible cap heights match Zebra Font 0's. Font *face* difference (Helvetica vs Font 0) is the remaining gap; only fix would be a TrueType upload via `^DU`, deliberately out of scope. |
| 22 | Reframed the page as a label-printing utility (no longer a "prototype"). Two new top-level pathways added: **Print custom ZPL** (paste arbitrary ZPL into a textarea, send via `device.send`; for ZPL pre-generated elsewhere — e.g. the OpenMRS printer module's server-side output) and **Print uploaded PDF** (file picker → `Zebra.Printer.getConvertedResource` → `device.send`; same conversion pipeline as pathway 2, reuses the PDF feature key). README §4 now documents all four pathways side-by-side. |
| 23 | Simplified the page around a single ZPL textarea and a server-rendered PDF preview. Pathways 1 + 3 collapse into one freely-edited textarea (default pre-filled assumes a 3"×2" label sized to the detected DPI; help text points at `^PW`/`^LL` for other media). Pathway 2's in-browser PDF construction (pdf-lib + JsBarcode) is replaced by a `POST /zpl-to-pdf` endpoint on the shim that shells out to a bundled [`zpl2pdf`](https://github.com/brunoleocam/ZPL2PDF) binary (MIT licensed, .NET self-contained, `BinaryKits.Zpl` renderer). The Generated PDF section is always visible and auto-renders on printer-select. Surveyed alternatives: [`zebrafy`](https://github.com/miikanissi/zebrafy) — rejected, only decodes embedded `^GF` bitmaps, not native primitives; Labelary HTTP API — rejected, ZPL contains patient data so a third-party server is unsuitable. The bundled binary is installed via `install-zpl2pdf.sh` (POSIX) / `install-zpl2pdf.ps1` (Windows), pinned to a specific release with SHA256 verification — not vendored in git (would bloat the repo by ~250 MB). |
| 24 | Defects-and-fixes that came out of bringing the redesign up. (a) zpl2pdf's offline-renderer `MediaBox` comes out in dots-at-the-DPI rather than points (PDF spec is points, 1 pt = 1/72 in), so a 3"×2" label produced a 12.5"×8.5" page. Workaround: shim post-processes via `gs -dPDFFitPage` to rescale to physical media size (parsed from `^PW`/`^LL` × 72 / dpi). (b) zpl2pdf's auto-detect path additionally pads the MediaBox ~1.5× wider/taller than its content, leaving the bottom-right ~33% empty — the gs rescale was carrying that padding through, making round-tripped prints come out at ~67% scale. Fix: pass `-w <in> -h <in> -u in` explicitly to zpl2pdf (same parse used for the gs target). (c) The first cut of the redesign populated the default ZPL's `^PW`/`^LL` from the printer's reported `cfg.printWidth` / `cfg.labelLength` — but those are the print HEAD width and last-calibrated label length, NOT the loaded media size. On wider-head printers (e.g. ZD-series with `^PW1280` head width), this made the hardcoded layout look "shifted left." Reverted to a hardcoded 3"×2" sized to the detected DPI; detection still populates the printer info card for diagnostics. |
| 25 | Replaced the "PDF feature key" textbox + "remember in localStorage" checkbox in the upload-PDF section with a server-side override file. The key applies to *both* PDF print pathways (Generated-PDF "Print this PDF" via `convertAndSendFileViaSdk`, and the upload-PDF shortcut via `getConvertedResource`) — burying the input under "Print uploaded PDF" was misleading. The key is also deployment configuration, not per-session UI state: localStorage is per-browser-profile, lost in incognito, and the "remember" checkbox is a mini-UX puzzle of its own. New design: at startup the page does `fetch('feature-key.txt')` from the same origin (i.e. `app/feature-key.txt`, gitignored, one line); if present and non-empty it overrides the bundled `DEFAULT_FEATURE_KEY` (Zebra's public demo key, kept for zero-config prototyping). The two PDF print paths now read the same module-level `featureKey` variable. Removed: `<input id="featureKey">`, `<input id="rememberKey">`, the entire localStorage block. The shim ignores the key either way (no license check), so on Linux + shim it's a no-op regardless. |
| 26 | Removed the bundled `DEFAULT_FEATURE_KEY` constant entirely (the entry-25 fallback). Two reasons: (a) committing a base64 string labelled "feature key" looks like distribution of licensed material, even though Zebra publishes the value openly on its own test harness; (b) anyone running Zebra's official helper in a production-shaped scenario should be getting a real key from Zebra rather than depending on a public demo key Zebra could rotate. New posture: `let featureKey = ''` at module level; if `app/feature-key.txt` is absent or empty, the two print sites suppress the `featureKey` option entirely (`{ featureKey } : {}`) so the SDK doesn't see an empty string. README §3 quickstarts for Windows/macOS now include an explicit "drop a key in `app/feature-key.txt`" step, with §4a documenting the public demo key location: Zebra publishes it at <https://developer.zebra.com/content/browser-print-pdf>, and the same value appears in plaintext in <https://cagdemo.com/BrowserPrint/test/external/zebra_test.html> view-source as the `feature_keys` JS variable. Linux + shim is unchanged: the shim ignores the key, so no setup is required for either PDF print path. **Correction to entry 15:** the `developer.zebra.com/forum/25874` citation there was apparently never accurate — that thread does not contain the key. The actual canonical Zebra docs page is `developer.zebra.com/content/browser-print-pdf`. |
| 27 | Filtered out devices with no `uid` from the dropdown. Tracked a Windows-specific reproducer where a user with an HP inkjet as Windows default printer + a USB-attached GX430t saw two entries in the printer dropdown: a phantom "undefined [undefined] ★ default" and the real Zebra. The page auto-selects index 0 (line 326), which was the phantom. Detection + status fired against it, every write came back with the SDK error "Failed to write to device: No value for name", and the status display ended in "error: status query timed out". The user could still print by manually picking the real Zebra and clicking Print, but the in-flight phantom queries contaminated the printer-info card (firmware never populated) and overwrote the real device's status. Root cause: Zebra's official Browser Print helper on Windows returns the OS default printer from `/default?type=printer` regardless of brand — `/available` correctly filters to Zebras, but `/default` doesn't. The bogus device record has neither `uid` nor `name`. Fix in `discover()`: drop entries from both `getDefaultDevice` and `getLocalDevices` results that lack a `uid` (a string with non-zero length), since they can't be addressed by the SDK anyway. Logs a warning when this happens so the user can tell why an expected printer is missing if it ever bites the other way. The shim's `/default` already returns null when no Zebra is registered, so this filter is a no-op there. |
| 28 | Three Windows-specific timing fixes for the official Browser Print helper, surfaced by a real-printer test session on a USB-attached GX430t. Field measurement: every bidirectional query through the official helper takes ~1500 ms even for small commands like `~hi`, vs. ~200–400 ms through our shim's read-until-quiet drain. (a) **getStatus timeout**. `STATUS_TIMEOUT_MS` was 500 ms — too tight; status timed out before the helper could reply. Bumped to 2500 ms; reduced retry count from 3 to 1 (worst case 5.2 s before error pill). (b) **/rediscover CORS noise**. `discover()` unconditionally posted to `/rediscover` to trigger a shim re-scan. Zebra's official helper 404s without CORS headers, which Chrome surfaces as a "blocked by CORS policy" console error even when our try/catch swallows the failure. Gated the call on `helperIsShim`, set by `refreshHelperInfo()` from `cfg.version` (the shim advertises `0.1.0-shim`; the official helper's version doesn't include "shim"). (c) **Status + detection queue contention**. `selectDevice` and the Refresh button both fired `refreshStatus()` and `detectPrinterCapabilities()` in parallel. The SDK has a single per-Printer command queue, so the four resulting queries (status + 3 detection) serialised inside the SDK regardless. With ~1500 ms/query on Windows, the queue took ~6 s to drain — and our `Promise.race` JS-side timeouts don't actually cancel the SDK call when they fire, so a JS-rejected query still blocked the queue until the helper's HTTP read returned. Symptoms: (i) `info.firmware` not populating because `getConfiguration` was 3rd in queue and tripped the 5 s detection timeout; (ii) clicking Refresh during in-flight detection piled a fresh status query onto a still-draining queue, which then timed out. Fix: introduced `refreshAll()` that awaits `refreshStatus()` then `await detectPrinterCapabilities()` sequentially; both call sites use it; the Refresh button's handler is single-flight (early-returns if `refreshInFlight` is true, disables the button while running). Also bumped `DETECT_TIMEOUT_MS` from 5 s to 12 s to absorb the worst-case wall-clock for the 3rd queued query plus future helper-side spikes. |
| 29 | Replaced synthetic placeholder values in the printer-info card with an explicit "querying…" loading state. The card has six fields: Connection, UID, Model, Firmware, Link-OS, Resolution. Connection and UID come from the SDK's Device handle synchronously and are real as soon as the user picks a printer. The other four require a bidirectional round-trip and used to display either a stale value, a fallback (Model showed `device.name` while detection was running), or a default (Resolution showed `300 dpi` because `densityDpi` defaulted to 300). The fallbacks were misleading on Windows where detection takes ~6 s end-to-end through Zebra's official helper — a user reasonably read "300 dpi" or "ZTC GX430t" as authoritative when in fact those were placeholders the page was about to overwrite. Worse, if detection failed for a specific field, the placeholder stayed visible. Fix: introduced `setPrinterInfoLoading()` which marks the four query-dependent fields as `querying…` (info-value muted styling) when refreshAll() starts. updatePrinterInfo() at the end of detection always sets each field to either the real value or `—` (no fallback to `device.name` for Model). `densityDpi` defaults to null instead of 300; consumers of `lastDetected.dpi` (regenerateDefaultZpl, generatePdfFromZpl) already coalesce with `|| <fallback>` so the ZPL still has a usable default. The pre-existing `clearPrinterInfo` and `updateInfoResolution` were updated to use the new `setInfoField` helper that preserves the `info-value` CSS class while toggling the `muted` modifier. |
| 30 | ASCII-only the PowerShell installer. A user reported `install-zpl2pdf.ps1` failing with cascading parser errors on Windows ("The string is missing the terminator: '.", "Missing closing '}'", "The Try statement is missing its Catch or Finally block"). Root cause: six lines used Unicode `…` (U+2026) and `—` (U+2014) and the file had no BOM. Windows PowerShell 5.1 — the default `powershell.exe` on Windows 10/11, which the README's `-ExecutionPolicy Bypass` Quickstart invokes — reads `.ps1` files in the system ANSI code page when no BOM is present. The UTF-8 multi-byte sequence `E2 80 A6` (ellipsis) gets reinterpreted as three ANSI characters; the closing `"` of the offending string is consumed inside that mojibake, the parser stays in string-mode, and every subsequent quote/brace cascades into a syntax error. PowerShell 7+ reads `.ps1` as UTF-8 by default so the bug didn't surface in any local testing — only on a real Windows machine using the default helper. Fix: replaced `…` → `...` and `—` → `-` throughout the script (six occurrences across `Write-Host`, `Write-Warning`, and `throw` strings). ASCII is portable across every PS version and every terminal code page — no BOM, no risk of further encoding mismatches when the file is touched by editors/tools that don't preserve BOM. The companion `install-zpl2pdf.sh` and the rest of the repo still use Unicode characters; bash doesn't apply encoding interpretation to source files (just passes bytes to the shell), and Markdown/HTML are inert from PowerShell's perspective. |
| 31 | Auto-discover the shim on a secondary port (9101) when Zebra's official Browser Print helper holds the primary 9100. A user reported `PermissionError: [WinError 10013] An attempt was made to access a socket in a way forbidden by its access permissions` when starting the shim on Windows alongside Browser Print. Root cause: Zebra's helper binds 9100 with `SO_EXCLUSIVEADDRUSE`, so a second process can't share it (Windows reports this as 10013 / WSAEACCES rather than the more familiar 10048 / WSAEADDRINUSE). README §4a previously hand-waved "run the shim on a different port and adjust the page's fetch URL", but the page hardcoded 9100 — there was no clean way for a Windows/macOS user to keep using Zebra's helper for printing AND get the shim's `/zpl-to-pdf` for the PDF preview. Fix: (a) page-side `probeSecondaryShim()` runs in `refreshHelperInfo()` after the primary 9100 returns. If the primary lacks ZPL→PDF support (i.e. it's Browser Print, not the shim), the page fetches `127.0.0.1:9101/config`; if a shim answers there, `pdfShimBaseUrl` flips to 9101 and `pdfPreviewSupported` becomes true. The `/zpl-to-pdf` POST in `generatePdfFromZpl` uses `pdfShimBaseUrl`. `/rediscover` keeps using `PRIMARY_HELPER_URL` since it only fires when `helperIsShim` is true (i.e. the shim is the primary, on 9100). (b) shim-side: wrap the `ThreadingHTTPServer(...)` constructor in a try/except that prints a friendly hint when the bind fails on the default 9100 — points the user at `--http-port 9101 --no-mdns` rather than dumping a stack trace. (c) README §3 quickstarts for Windows and macOS now instruct `python ... --http-port 9101 --no-mdns`; §4a explains the alongside-helper pattern explicitly with the WinError 10013 cause. The fallback path was deliberately limited to 9101 — a single hardcoded secondary — rather than a config-file or env-var to keep the zero-config "drop the shim alongside Browser Print and it works" experience. If users need a non-9101 setup, they'd need to edit the page's `SECONDARY_SHIM_URL` constant; I'll revisit if anyone actually hits that. |
| 32 | Consolidated three half-baked config sources into one `app/config.json` consumed by both the page (via fetch from the served origin) and the shim (via filesystem). Before: `app/feature-key.txt` (one-line key, page-only), an in-progress `app/pdf-shim-port.txt` proposal (one-line port, page-only), and `utils/printers.json` (network printer list, shim-only) — three separate file formats, three separate parsers, no shared schema. The fragmentation meant the shim couldn't pick up the user's port choice automatically (entry 31 worked around this with a hardcoded 9101 fallback) and adding any new config concern would have meant a fourth file. New schema: `{shimPort, featureKey, networkPrinters}` — JSON, all keys optional. Page reads `shimPort` (where `/zpl-to-pdf` POSTs go) and `featureKey` (passed to `convertAndSendFile`); shim reads `shimPort` (default for `--http-port`) and `networkPrinters` (replaces the old `network` key in `printers.json`). The shim ignores `featureKey` (no license check) but having it in the same file is harmless — a single source of truth is worth more than per-consumer file separation. Both ends fall through to defaults silently if the file is missing or unparseable. The CLI `--http-port` and `--network` flags still override file values when given. JSON over YAML because the shim is stdlib-only (no PyYAML). The bind-error hint message now points at `app/config.json` and includes the `netsh` recipe for diagnosing Hyper-V port reservations. The standalone `feature-key.txt` and `printers.json` paths were removed without a migration period — the repo is still pre-publish, no deployed users to break. |
| 33 | Corrected the recommended `shimPort` from 9101 to 9102. Entries 31 and 32 told Windows/macOS users to put the shim at 9101, on the assumption that 9101 was free and Browser Print held only 9100. Field-confirmed wrong: a user running Zebra's Browser Print on Windows reported `WinError 10013` on 9101, with `netstat -ano \| findstr "9100 9101"` showing both ports held by the same Java PID. Re-reading the SDK source confirms it: `BrowserPrint-3.1.250.min.js` has `var m="http://127.0.0.1:9100/"; q && "https:"===location.protocol && (m="https://127.0.0.1:9101/")` — the helper is one process listening on both 9100 (HTTP) and 9101 (HTTPS), and Windows binds with SO_EXCLUSIVEADDRUSE so neither is shareable. Initial Hyper-V/WSL2 reservation hypothesis was wrong (the user's `netsh interface ipv4 show excludedportrange protocol=tcp` showed only 5357 and 50000-50059, neither overlapping 9101). Fix: README §3 quickstarts use 9102 in the `app/config.json` example; §4a's `shimPort` subsection now leads with the dual-port binding (with `netstat`/`lsof` recipes for verification) and demotes the Hyper-V caveat to a secondary case. Shim's bind-error hint reordered to put dual-port first. Decision-log entry 5 (which documented Browser Print's two-port listening from the start) stays — the original observation was correct; we just hadn't connected it to the alongside-helper port-choice question until now. |
| 34 | Symmetric `shimHttpsPort` config alongside `shimPort`. Entry 33 fixed the HTTP port collision (Browser Print holds both 9100 and 9101 on Win/Mac), but the shim's own `DEFAULT_HTTPS_PORT = 9101` had the same conflict latent — a user passing `--https` while Browser Print was running would hit `WinError 10013` on the HTTPS bind too, with no config-file way to override. Asymmetric: `shimPort` was settable in `app/config.json`; `--https-port` was CLI-only. Fix: added `shimHttpsPort` to the schema (default 9101). Both the page (loadConfig) and the shim (parse_args, pre-config pass) read it. The page picks scheme + port based on its own `location.protocol` — HTTP page uses `http://...:shimPort`, HTTPS page uses `https://...:shimHttpsPort` — matching the Browser Print SDK's own auto-switch. Conventional pair when running alongside Browser Print is now 9102 (HTTP) / 9103 (HTTPS). 7/7 unit-test cases pass: missing file × HTTP/HTTPS, shimPort-only × HTTP/HTTPS, both ports × HTTP/HTTPS, invalid shimHttpsPort. The shim's bind-error hint already covered HTTPS (it switches "HTTP"/"HTTPS" via the `scheme` variable from ssl_ctx) — no rewrite needed there. |
| 35 | Two issues a Windows user hit running the shim alongside Browser Print on port 9102: (a) PDF preview still showed "PDF preview unavailable" in the page even though the shim was correctly running; (b) startup logs were full of warnings irrelevant to the alongside-Browser-Print role on Windows ("PDF CONVERSION: DISABLED — Ghostscript not found", "No USB printers found under /dev/usb/lp*"). **Issue (a)** — page-side bug: `refreshHelperInfo()` had a `pdfShimBaseUrl = PRIMARY_HELPER_URL` line that reset the URL to 9100 *after* `loadConfig()` had correctly set it to `http://127.0.0.1:9102`. `probeShim()` then read the reset value, fetched 9100/config (Browser Print's), saw no "shim" in the version, concluded no shim was there. Leftover from an earlier iteration before probeShim was switched to use `pdfShimBaseUrl` directly. Fix: removed the reset; `loadConfig()` is the single source of truth for the URL. **Issue (b)** — shim-side noise: introduced `IS_LINUX = sys.platform.startswith('linux')` constant. `discover_usb_printers()` returns immediately on non-Linux (no log line) — `/dev/usb/lp*` is structurally Linux-only, the warning was suggesting an action that's structurally impossible on Win/Mac. The Ghostscript-missing warning is now gated: still WARNING on Linux (where the shim is the primary helper and missing GS breaks /convert), demoted to INFO with explanatory text on Win/Mac (where Browser Print handles its own PDF→ZPL and missing GS is expected). The "No printers registered" wall of warnings is similarly gated: still fires on Linux with all the registration suggestions; replaced on Win/Mac with one INFO line acknowledging that BP presumably handles printer enumeration and this shim is just serving /zpl-to-pdf. Linux output unchanged; simulated-Windows startup is now two clean INFO lines. |
| 36 | Diagnosed a "licensing key and none was provided" report from a Windows user who *had* set `featureKey` in `app/config.json`. Root cause was almost certainly PowerShell 5.1's `Set-Content` defaulting to UTF-16 LE with BOM — `fetch()` decodes responses as UTF-8, `JSON.parse` gets garbage, the page falls through to "no key" without an obvious error in the page log. Fix is two-pronged. **Diagnostic:** rewrote `loadConfig()` to read the file as bytes, sniff the first two bytes for UTF-16 BOMs (`FF FE` / `FE FF`) and emit a verbatim "re-save with `Set-Content -Encoding UTF8`" message; decode UTF-8 with `fatal:true` (rejects partial sequences); strip a UTF-8 BOM if present; on JSON parse failure log the first 80 chars so a smart quote / missing brace is visible without DevTools. The featureKey-loaded log now reports the char count so the user can confirm their key reached the page (`PDF feature key loaded (107 chars)`). The print-PDF error handler distinguishes "none was provided" (SDK got `{}` — actionable hint depending on whether we loaded a key) from "invalid/expired" (SDK got a key but rejected it); the pre-print log records featureKey state + PDF size, so post-mortem from a user-pasted log is one line. **Documentation:** README §3 Windows Quickstart now uses `Set-Content -Encoding UTF8` (the `-Encoding` flag is mandatory on PS 5.1 — the default of UTF-16 LE silently breaks `fetch+JSON.parse`); §4a "Runtime config" subsection has an "Encoding gotcha (Windows)" paragraph pointing at the new page-log line. |
| 37 | Same Windows user reported the shim emitting `INFO No printers registered with this shim. Browser Print presumably handles printer enumeration...` every 30 seconds. The 30 s rescan exists primarily so a Linux operator who plugs in a USB Zebra mid-run sees it picked up without restart, and so an mDNS-advertised network Zebra that joins the LAN later gets registered. On Windows/Mac the shim is purely a /zpl-to-pdf side-channel and there's nothing to discover at all — but the rescan still ticked, hit the empty-state branch in `refresh_devices`, and re-logged the same INFO. Several adjacent log lines had the same "fires every rescan" property: `Discovered USB printer: %s`, `Configured network printer: %s @ %s:%s`, `Configured network printer (CLI): %s @ %s:%d`, `Discovered network printer (mDNS): %s @ %s:%d`, plus the Linux-only `No USB printers found under /dev/usb/lp*` fallback. Fix: track previous registered-uid set in module-level `_PREV_REFRESH_UIDS` (None = "no refresh yet"); after each refresh compute `added_uids = new − prev` and `removed_uids = prev − new`; only log on first call OR when added/removed is non-empty. Per-printer "registered" / "no longer registered" lines move into `refresh_devices` (the discovery helpers no longer log per-printer; they only log actionable errors like missing /dev access or a bad config entry). The empty-state block (Linux warning wall vs. non-Linux single INFO) was extracted to `_log_no_printers(args)` and only fires from the same diff-gated branch. Rescan cadence (`--rescan-seconds`, default 30 s) was left alone — silent rescans are cheap, and on Linux the cadence is the whole point of the timer. /rediscover already logs its own "re-scanning USB + mDNS network printers" before delegating to refresh_devices, so the user-triggered path still gets feedback even when the resulting set is unchanged. Verified with a 7-case smoke test: empty first-call logs, rescan silent, add logs, post-add rescan silent, removal logs (with re-emitted empty-state), non-Linux empty-state is single INFO, non-Linux rescan silent. |
| 38 | After entry 36's diagnostics shipped, the Windows user re-tested and the page log now showed `featureKey present (86 chars)` — i.e. the key WAS reaching the page — yet the very next line was `print pdf failed: The format conversion attempted requires a licensing key and none was provided`. 86 chars matches Zebra's public PDF demo key from cagdemo's test harness (`AbsQM…`, full value visible on view-source of that page), so the key value was correct. **Root cause:** wrong wire-shape. The page was passing `{ featureKey: "..." }` as the SDK options dict. Zebra's helper looks for `options.keys.<fromFormat>` — i.e. `{ keys: { pdf: "..." } }` — so a flat `featureKey` is silently dropped and the helper rightly says "none was provided." The SDK is transparent: the strings `featureKey`, `licenseKey`, `keys` are all *absent* from `BrowserPrint-3.1.250.min.js` and its Zebra extension; `f.convert` does `var t = {}; t.options = userOptions; xhr.send(formData(json=JSON.stringify(t), blob))` — whatever name you put in your options object is the literal name the helper sees. The original `featureKey` name in our code probably came from misreading old forum posts / a third-party TypeScript wrapper (`brianzinn/browserprint-es`) that invented `featureKey?: string` as a convenience type but forwards options verbatim. **Fix:** verified canonical shape against Zebra's own reference page (cagdemo `BrowserPrint/test/external/zebra_test.html`, source line 11: `var feature_keys = {"pdf": "..."}`, every call site uses `{keys: feature_keys}` — including a deliberate negative test `printPDFAsLabelBadKey` calling `{keys: {"pdf":"badkey"}}`, smoking-gun confirmation of the schema). Two SDK call sites in the page now wrap: `convertAndSendFileViaSdk` does `device.convertAndSendFile(blob, res, rej, featureKey ? { keys: { pdf: featureKey } } : {})`; the upload-PDF path `getConvertedResource(file, { toFormat, fromFormat, ...(featureKey ? { keys: { pdf: featureKey } } : {}) })`. The `app/config.json` schema stays as a bare `featureKey` string — wrapping is a one-line wire concern, exposing `{keys: {pdf: ...}}` to operators would be unfriendly. The print-PDF error hint now correctly identifies "none was provided + key was loaded" as a wire-shape regression rather than a config-name mismatch. README §4a's `featureKey` subsection documents the wrap explicitly so a future contributor doesn't unwrap it; the in-code comment in the SDK call site cites the cagdemo reference URL. |
| 39 | Per-printer config consolidated under a single `printers[]` array, replacing the network-only `networkPrinters` and adding three new concerns: (a) friendly display name override for the dropdown — Browser Print on Windows can return opaque/ugly names depending on how the printer was installed; (b) per-printer label dimensions to drive both `defaultZpl`'s `^PW`/`^LL` and `fitToFromZplTextarea`'s fallback for upload-PDF scaling — neither was reliably derivable from the printer (printer.printWidth is print-HEAD width not loaded media; labelLength is last-calibrated drift state, not authoritative; both can lie on wider-head printers per entry 24); (c) a single point of entry for adding more per-printer settings later without splintering the schema again. **Schema:** each entry has either `host`+`port` (network printer; shim registers as `NetworkDevice`, page derives `net:<host>:<port>` as the lookup key) or `uid` (page-only metadata for printers discovered by other means — Browser Print on Win/Mac, USB on Linux). Optional fields: `name` (display override), `labelWidth`/`labelHeight`/`labelUnits` (`"in"` default, `"mm"` accepted; bad units / negative dims warn + skip dims but keep the entry's `name`). Bad/half-set/no-key-no-host entries warn + skip with the rest of the config still loading — same shape as the prior `networkPrinters` validation. **Page wiring:** `defaultZpl({ dpi, widthIn, heightIn })` takes dims; `regenerateDefaultZpl` looks up `device.uid` against the loaded `Map`; `fitToFromZplTextarea` uses the same map for its 3×2" fallback (textarea `^PW`/`^LL` still wins when present, so user edits remain authoritative); dropdown rendering precedence is `cfg.name` → `d.name` → `d.uid` → `'?'` (the last-resort uid covers the user-reported "ugly UID in dropdown" symptom whose underlying cause was Browser Print returning an empty `name` for some Windows printer installs). New `Label` row in the Printer Info card shows resolved size + provenance (`3 × 2 in (config.json)` vs `3 × 2 in (default)`) so an operator can verify their config entry matched. **Shim wiring:** `load_network_printers` now reads `cfg['printers']` and only registers entries with a `host` field — uid-keyed entries are silently ignored as page concerns. Pre-publish so no migration period; rename is hard. **Verified:** 9-case Node-driven page-parser smoke test (network entry by host+port; UID entry inches; UID entry mm-converted; name-only no-dims; bad units; negative dims; missing key; half-set dims; non-object) — 7 valid entries loaded, all 5 expected warnings emitted; 4-case Python shim-loader smoke test (network with port; network without port → defaults to 9100; uid-only ignored; bad host-without-name warns) — registers exactly the network entries and skips the rest. README §4a's per-entry table documents which fields are required when, and which side reads what; decision-log entry 32 is now historically correct but superseded by this. |
| 40 | Drafted `lib/zebra-browser-print.js`: a vanilla-JS, MPL-2.0-licensed minimal client for Zebra Browser Print's HTTP API. Scope: discovery (`getDefaultDevice`/`getLocalDevices`), helper config (`getApplicationConfiguration`), per-device `send`/`read`, plus a `sendAndRead` helper that drains-until-quiet for multi-frame replies. NOT included: `convertAndSendFile`/`getConvertedResource` (PDF→ZPL via helper licensing — most production webapps generate ZPL server-side); per-printer command queue (caller serialises); callback dual API (Promise-only); the `Zebra.Printer` wrapper with `getStatus`/`getConfiguration`/etc. (each is a single `sendAndRead` + app-specific parsing). Wire format derived from the documented HTTP contract — re-implemented from the same observed shapes that drove `utils/browser-print-shim.py` (which serves as a working server-side reference). No Zebra source consulted; no Zebra binary bytes copied. ~130 lines of actual code; total 274 with the MPL header, JSDoc-style block, and inline rationale. Companion `lib/README.md` documents scope, usage (script tag + ESM), and license posture; `lib/zebra-browser-print.test.mjs` runs 13 cases that mock `globalThis.fetch` and assert URL/method/body for every endpoint plus the drain-until-quiet timing on `sendAndRead`. The fetch-mock approach means tests run in ~250 ms with no live helper or printer — and the wire shapes asserted are the same ones the shim implements, so passing tests + a working shim ≈ passing against real Browser Print. Phase 1 deliberately stops here so the OpenMRS module side has a clean handoff target without first requiring a decision on PDF conversion / queue semantics. Phase 2 (when this prototype tool itself wants to retire the bundled .min.js files) extends with `^HH` config parsing, `~hs` status parsing, and `convertAndSendFile`; an estimated ~150 more lines on top of Phase 1. Located under `lib/` (not `app/`) to make the licensing posture clean: `app/` may still bundle Zebra's proprietary SDK for prototype use, `lib/` is unambiguously MPL and has no proprietary dependencies. |
| 41 | Phase 2: extended `lib/zebra-browser-print.js` with the rest of the proprietary SDK surface this prototype uses, then migrated `app/browser-print.html` to load the MPL client instead of the two bundled `BrowserPrint-*.min.js` files. New in the lib: `Device.convertAndSendFile(blob, options)` (multipart POST to `/convert` with `{options, device}` JSON + blob, action="print" by default, fromFormat auto-derived from the blob's MIME type the same way the proprietary SDK does — strips `image/`, `application/`, and `x-ms-` prefixes); `Status` class with `isPrinterReady()` / `getMessage()`, plus typed flags for paper-out / paused / head-open / ribbon-out / temp under-over / receive-buffer-full; `Configuration` shape with `printWidth` / `labelLength` (numbers) + `firmwareVersion` / `linkOSVersion` (strings) + `settings` (every `^HH` row keyed by its right-aligned label); `Info` shape from `~hi` with `model` (the field carrying the dpi-suffix used as last-resort dpi detection); `Printer` class wrapping a Device with `getStatus` / `getConfiguration` / `getInfo` / `getSGD`. Parsers handle the wire shapes: STX/ETX framing for `~hs` (three frames; <3 frames marked offline=true to mirror the SDK's transient-bus-buffer race semantic), `! U1 getvar "name"\r\n` for SGD with quoted-reply unwrapping, ^HH multi-line text where each row is `<value>` + `\s{2,}` + right-aligned label. Notable parser detail: the regex on the ^HH config dump uses `(.+\S)\s{2,}` (greedy on the value) instead of `(.+?)\s{2,}` (non-greedy) — the FIRMWARE row often has internal double-spaces (`6.0  6.0.5 <-          FIRMWARE`) that a non-greedy match splits on, capturing only `6.0` as the value; greedy + backtracking finds the *last* `\s{2,}` gap, which is the right semantic. Caught by the test suite. Test suite extended from 13 to 36 cases: 5 for convertAndSendFile (multipart wire shape, fromFormat auto-derive incl. `x-ms-` prefix, explicit fromFormat preserved, JSON-envelope response parse, non-Blob rejection), 5 for the status parser (ready / paper-out / head-open / short-read offline / empty input), 2 for the config parser, 2 for info, 4 for SGD, 5 for the Printer wrapper (one per method plus constructor type-check). Mocked-fetch only — ~250 ms total. Page migration: removed the callback→Promise wrappers (`promisify`, `devSend`, the `bp*` ones now thin aliases), replaced `new Zebra.Printer(d)` with `new ZebraBrowserPrint.Printer(d)`, replaced `new Promise((res,rej) => device.convertAndSendFile(blob, res, rej, opts))` with `device.convertAndSendFile(blob, opts)` at both call sites, replaced the diagnostic-section `device.send(bytes, res, rej)` callback with the promise form. Two `<script src="BrowserPrint-...">` tags collapsed to one `<script src="../lib/zebra-browser-print.js">`. The `../lib/` reach across directories means the page can no longer be served with `python -m http.server 8000 -d app` — README §3 quickstarts (Linux/Windows/macOS) updated to drop `-d app` and the URL path now includes `/app/`. Bundled `BrowserPrint-3.1.250.min.js` and `BrowserPrint-Zebra-1.1.250.min.js` are now unreferenced; left in place for this commit so the user can validate Phase 2 on real hardware before they're removed in a follow-up. |
| 42 | User reported intermittent garbled Model/Firmware in the Printer Info card after clicking Refresh on Ubuntu+shim — sometimes correct, sometimes blank, once a stray bracket character. Root cause: missing per-Device command queue in `lib/zebra-browser-print.js`. Phase 2 (entry 41) deliberately omitted the queue ("caller serializes — most have one operation in flight at a time anyway") but the prototype's `detectPrinterCapabilities` fires three queries concurrently via `Promise.allSettled([getSGD, getConfiguration, getInfo])`, relying on the SDK's per-Printer queue without realising it. Without the queue: three concurrent `sendAndRead` calls each `POST /write` and `POST /read` against the same shim; the shim's `ThreadingHTTPServer` runs each request in its own thread, so the writes serialise nondeterministically on the device's lock and the three reads race for whichever bytes are buffered. One query's reply ends up parsed by another query's parser — the `~hi` info parser sees status frame bytes (`000,0,0,1240`) where it expects `ZTC GX430t-300dpi-ZPL`, producing blank Model fields, and stray STX/ETX boundaries leak across (the "stray bracket character"). Why this didn't show on Windows+Browser Print: Zebra's Java helper appears to serialise per-device internally, masking the contention. **Fix:** added per-Device command queue. The proprietary SDK had this on the Printer wrapper; we put it on Device so raw `device.send`/`device.read` from outside any Printer (e.g. the diagnostic panel) also benefits. Implementation: each public method (`send`, `read`, `sendAndRead`, `convertAndSendFile`) wraps its body in `_enqueue(op)`, which chains the op onto `this._queue` (a Promise that resolves after each op completes). Errors don't break the chain — the next op fires either way (otherwise a single failed query would freeze the queue forever). To avoid deadlock when `sendAndRead` internally awaits `send` and `read`, the public methods delegate to private `_sendImpl` / `_readImpl` impls that bypass the queue; `sendAndRead`'s body uses those directly since it already holds the queue slot. Also extracted `_sendAndReadImpl` and `_convertAndSendFileImpl` for symmetry. Three new test cases verify (a) three concurrent `sendAndRead` calls serialise — wire ordering is `write A → read → write B → read → write C → read`, not interleaved; (b) a thrown error in op A doesn't freeze the queue for op B; (c) the queue serialises across all four methods (mixed `send`+`read`+`sendAndRead`+`convertAndSendFile`). Test count 36 → 39, all passing. |
| 43 | User reported on Windows + Browser Print: status pill stuck on "Error: status query timed out" and Model field blank, even though Firmware / Resolution / UID / Connection / Label populated correctly. Two bugs, both timing-related. **(1) CORS preflight on every helper request.** Phase 1's `_request` set `Content-Type: application/json` on every JSON POST. That's a non-simple request by CORS, so the browser fires `OPTIONS` before every `POST /write` and `POST /read`. On Windows + Browser Print each round-trip is ~750 ms; doubling that pushes status calls past the page's STATUS_TIMEOUT_MS=2500 ms. The proprietary SDK uses `xhr.send(JSON.stringify(...))` with no explicit Content-Type — XHR defaults to `text/plain;charset=UTF-8` for string bodies, which is a simple-request type and skips the preflight. Helper's body parser doesn't check Content-Type, just JSON-decodes the body — verified in `utils/browser-print-shim.py:_read_json` (no header check). Fix: switch `_request`'s Content-Type to `text/plain;charset=UTF-8`. **(2) `sendAndRead` had no terminator-based fast-exit.** The proprietary SDK's `executeRequest` for `info`/`config`/`status` types calls `sendThenReadUntilStringReceived(cmd, '\x03', ...)` — exits the moment ETX (0x03) lands in the buffer. Our `sendAndRead` only had drain-until-quiet (idleQuietMs after last data) + a hardCapMs fallback; on a slow Windows helper with hardCapMs=1500 ms and `~hi`/`~hs` taking ~1500 ms, the read either landed data right at the cap (lucky) or returned empty (unlucky → blank Model). Fix: added `opts.until` to `sendAndRead`. When set, the loop exits as soon as the accumulated buffer contains the terminator; saves the trailing idleQuietMs wait. `Printer.getStatus`/`getInfo`/`getConfiguration` now pass `until: '\x03'` (matching the SDK's switch on query type); `getSGD` keeps drain-until-quiet because some firmwares omit framing on getvar replies. Why the user saw firmware/resolution/UID succeed but status/info fail: getConfiguration had hardCapMs=4000 explicit (already bumped for slow ^HH dumps), getSGD's response is short enough to land within hardCap, getStatus is wrapped in the page's tight 2500ms timer (so the slow round-trip rolled past it), getInfo is wrapped in the lenient 12000ms DETECT_TIMEOUT_MS but our hardCap=1500 fired first leaving an empty buffer. Three new test cases: (a) JSON POSTs use a simple Content-Type — regex-asserts text/plain or other CORS-simple types and explicitly bans application/json; (b) sendAndRead exits on `until` terminator in <200 ms even with idleQuietMs=300 — proves fast-exit is wired; (c) sendAndRead WITHOUT `until` still drains until quiet — guards against breaking the SGD path. Existing test that asserted `Content-Type: application/json` updated to expect `text/plain;charset=UTF-8`. Test count 39 → 42, all passing. |
